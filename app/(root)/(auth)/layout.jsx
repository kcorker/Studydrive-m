export const metadata = {
  title: "Login | College Notes",
  description: "study material web app for students",
};

export default function AuthLayout({ children }) {
  return <section>{children}</section>;
}
